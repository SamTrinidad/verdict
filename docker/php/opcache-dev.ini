; Development override — disable OPcache entirely so file edits take effect immediately.
; Docker Desktop on Windows has unreliable file timestamp detection, making
; validate_timestamps=1 insufficient. Disabling is the reliable solution for local dev.
opcache.enable=0
opcache.enable_cli=0
